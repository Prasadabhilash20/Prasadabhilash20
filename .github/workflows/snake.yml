name: Generate Snake

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Generate Snake
        uses: Platane/snk@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-snake.svg?color_snake=%230EA5E9&color_dots=%23BFE4FE,%237DD3FC,%2338BDF8,%230EA5E9,%230369A1
            dist/github-snake-dark.svg?palette=github-dark&color_snake=%2338BDF8&color_dots=%23BFE4FE,%237DD3FC,%2338BDF8,%230EA5E9,%230369A1

      - name: Push Snake to output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
